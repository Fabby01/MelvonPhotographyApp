@page
@model MRMstudios.Pages.BookingModel
@{
    ViewData["Title"] = "Book a Session";
}

<section class="booking-hero">
    <div class="container">
        <h1 class="booking-title">Book Your Photography Session</h1>
        <p class="booking-subtitle">Choose your service and preferred date. We'll handle the rest.</p>
    </div>
</section>

<div class="container page-section">
    <div class="row justify-content-center">
        <div class="col-12 col-xl-11">
            @if (!string.IsNullOrEmpty(Model.Message))
            {
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    <div><strong>Booking Successful</strong></div>
                    <div>@Model.Message</div>
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            }

            @if (!string.IsNullOrEmpty(Model.Error))
            {
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                    <div><strong>Booking Error</strong></div>
                    <div>@Model.Error</div>
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            }

            <div class="booking-layout">
                <div class="booking-calendar-section">
                    <div class="calendar-container">
                        <div class="calendar-header">
                            <button id="prevMonth" class="calendar-nav-btn" type="button">&larr;</button>
                            <h3 id="currentMonth" style="flex: 1; text-align: center; margin: 0; font-size: 1.1rem; font-weight: 700;"></h3>
                            <button id="nextMonth" class="calendar-nav-btn" type="button">&rarr;</button>
                        </div>

                        <div class="calendar-weekdays">
                            <div class="weekday">Sun</div>
                            <div class="weekday">Mon</div>
                            <div class="weekday">Tue</div>
                            <div class="weekday">Wed</div>
                            <div class="weekday">Thu</div>
                            <div class="weekday">Fri</div>
                            <div class="weekday">Sat</div>
                        </div>

                        <div class="calendar-dates" id="calendarDates"></div>

                        <div class="calendar-legend">
                            <div class="legend-item">
                                <span class="legend-color available"></span>
                                <span>Available</span>
                            </div>
                            <div class="legend-item">
                                <span class="legend-color unavailable"></span>
                                <span>Unavailable</span>
                            </div>
                            <div class="legend-item">
                                <span class="legend-color selected"></span>
                                <span>Selected</span>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="booking-form-section">
                    <form method="post" class="booking-form">
                        <div class="form-section">
                            <h3 class="section-title">Contact Information</h3>
                            <div class="form-grid-2">
                                <div>
                                    <label for="fullName" class="form-label">Full Name <span class="required">*</span></label>
                                    <input type="text" class="form-control" id="fullName" asp-for="Booking.FullName" placeholder="John Doe" required>
                                    <span asp-validation-for="Booking.FullName" class="text-danger" style="font-size: 0.85rem; margin-top: 0.25rem; display: block;"></span>
                                </div>

                                <div>
                                    <label for="email" class="form-label">Email Address <span class="required">*</span></label>
                                    <input type="email" class="form-control" id="email" asp-for="Booking.Email" placeholder="john@example.com" required>
                                    <span asp-validation-for="Booking.Email" class="text-danger" style="font-size: 0.85rem; margin-top: 0.25rem; display: block;"></span>
                                </div>
                            </div>

                            <div class="mt-3">
                                <label for="phone" class="form-label">Phone Number <span class="required">*</span></label>
                                <input type="tel" class="form-control" id="phone" asp-for="Booking.PhoneNumber" placeholder="+46 70 841 7437" required>
                                <span asp-validation-for="Booking.PhoneNumber" class="text-danger" style="font-size: 0.85rem; margin-top: 0.25rem; display: block;"></span>
                            </div>
                        </div>

                        <div class="form-section">
                            <h3 class="section-title">Session Details</h3>
                            <div>
                                <label for="serviceType" class="form-label">Service Type <span class="required">*</span></label>
                                <select class="form-select" id="serviceType" asp-for="Booking.ServiceType" required>
                                    <option value="">Choose a service...</option>
                                    @foreach (var service in Model.Services)
                                    {
                                        <option value="@service.Name">
                                            @service.Name - $@service.Price
                                        </option>
                                    }
                                </select>
                                <span asp-validation-for="Booking.ServiceType" class="text-danger" style="font-size: 0.85rem; margin-top: 0.25rem; display: block;"></span>
                            </div>

                            <div class="mt-3">
                                <label for="preferredDate" class="form-label">Preferred Session Date <span class="required">*</span></label>
                                <input type="date" class="form-control" id="preferredDate" asp-for="Booking.PreferredDate" required>
                                <span asp-validation-for="Booking.PreferredDate" class="text-danger" style="font-size: 0.85rem; margin-top: 0.25rem; display: block;"></span>
                                <small style="color: var(--muted); display: block; margin-top: 0.5rem;">Minimum 7 days in advance</small>
                            </div>
                        </div>

                        <div class="form-section">
                            <h3 class="section-title">Special Requests</h3>
                            <label for="specialNotes" class="form-label">Any special requests or ideas?</label>
                            <textarea class="form-control" id="specialNotes" asp-for="Booking.SpecialNotes" rows="3" placeholder="Tell us about your vision, location preferences, style, or any special details."></textarea>
                        </div>

                        <button type="submit" class="btn btn-primary w-100" style="min-height: 56px; font-size: 1.05rem;">
                            Submit Booking Request
                        </button>

                        <p class="text-center mt-3 mb-0" style="color: var(--muted); font-size: 0.9rem;">
                            You'll receive a confirmation email shortly after submitting.
                        </p>
                    </form>
                </div>
            </div>

            <div class="help-section mt-5">
                <h4 style="margin-bottom: 1rem; font-size: 1.1rem;">Have Questions?</h4>
                <p class="mb-0">We're happy to help. <a href="/Contact">Contact us directly</a> if you have any questions about services, pricing, or availability.</p>
            </div>
        </div>
    </div>
</div>

<script>
    const availableDatesStr = '@string.Join(",", Model.AvailableDates.Select(d => d.ToString("yyyy-MM-dd")))';
    const availableDates = new Set(availableDatesStr.split(',').filter(d => d));

    let currentDate = new Date();
    const dateInput = document.getElementById('preferredDate');

    function renderCalendar() {
        const year = currentDate.getFullYear();
        const month = currentDate.getMonth();

        document.getElementById('currentMonth').textContent =
            currentDate.toLocaleDateString('en-US', { month: 'long', year: 'numeric' });

        const firstDay = new Date(year, month, 1);
        const startDate = new Date(firstDay);
        startDate.setDate(startDate.getDate() - firstDay.getDay());

        const calendarDates = document.getElementById('calendarDates');
        calendarDates.innerHTML = '';

        let currentCell = new Date(startDate);
        for (let i = 0; i < 42; i++) {
            const dateDiv = document.createElement('div');
            dateDiv.className = 'calendar-date';

            const dateStr = currentCell.toISOString().split('T')[0];
            const isCurrentMonth = currentCell.getMonth() === month;
            const isAvailable = availableDates.has(dateStr);
            const isSelected = dateInput.value === dateStr;

            if (!isCurrentMonth) {
                dateDiv.className += ' other-month';
            } else if (isSelected) {
                dateDiv.className += ' selected';
            } else if (isAvailable) {
                dateDiv.className += ' available';
                dateDiv.addEventListener('click', () => {
                    dateInput.value = dateStr;
                    renderCalendar();
                });
            } else {
                dateDiv.className += ' unavailable';
            }

            dateDiv.textContent = currentCell.getDate();
            calendarDates.appendChild(dateDiv);
            currentCell.setDate(currentCell.getDate() + 1);
        }
    }

    document.getElementById('prevMonth').addEventListener('click', () => {
        currentDate.setMonth(currentDate.getMonth() - 1);
        renderCalendar();
    });

    document.getElementById('nextMonth').addEventListener('click', () => {
        currentDate.setMonth(currentDate.getMonth() + 1);
        renderCalendar();
    });

    dateInput.addEventListener('change', () => {
        if (dateInput.value) {
            currentDate = new Date(dateInput.value);
            renderCalendar();
        }
    });

    renderCalendar();
</script>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
